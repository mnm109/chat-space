.side__bar
  .side__bar__header
    .side__bar__header__box
      .side__bar__header__box__user
        = current_user.name
      .side__bar__header__box__menu__box
        =link_to new_group_path , class: "menu__box__btn" do
          = icon('far', 'edit')
        =link_to edit_user_path(current_user), class: "menu__box__btn" do
          = icon('fas', 'cog')
  .side__bar__group
    - current_user.groups.each do |group|
      .side__bar__group__list
        = link_to group_messages_path(group), class: "group__list" do
          .side__bar__group__list__title
            = group.name
          .side__bar__group__list__message
            メッセージはまだありません。